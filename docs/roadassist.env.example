# /etc/roadassist/env
# Copy to /etc/roadassist/env, fill in values, then: chmod 640 /etc/roadassist/env && chown root:roadassist /etc/roadassist/env

JWT_SECRET=change-this-to-a-long-random-secret

DB_MODE=sqlite
DB_PATH=/var/lib/roadassist/roadassist.db
